# Server
PORT=3001
HOST=0.0.0.0
LOG_LEVEL=info
CORS_ORIGIN=*

# GitHub — create a fine-grained token with read access to your repos
# https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your_token_here

# Policy directory (defaults to ./policies relative to project root)
POLICY_DIR=./policies
